<view class='view'>
  <map id="map" longitude="{{quick_map.longitude}}" latitude="{{quick_map.latitude}}" markers="{{markers}}" scale="18" show-location style="width: 100%; height: 100%;"></map>
</view>